import { IP_POOL_NAME, UNSUBSCRIBE_GROUPS } from './mail.sendgrid.const';

describe('IP_POOL_NAME', () => {
  it('should exist when imported', () => {
    // arrange
    // act
    // assert
    expect(IP_POOL_NAME).toBeDefined();
  });
});

describe('UNSUBSCRIBE_GROUPS', () => {
  it('should exist when imported', () => {
    // arrange
    // act
    // assert
    expect(UNSUBSCRIBE_GROUPS).toBeDefined();
  });
});
