import { REDIS_DELIMITER } from './redis.consts';

describe('REDIS_DELIMITERs', () => {
  // arrange
  // act
  // assert
  it('should be defined when imported', () => {
    expect(REDIS_DELIMITER).toBeDefined();
  });
});
