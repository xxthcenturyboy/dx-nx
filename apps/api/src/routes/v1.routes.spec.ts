import { RoutesV1 } from './v1.routes';

describe('RoutesV1', () => {
  // arrange
  // const routes = new RoutesV1();
  // act
  // assert
  it('should exist when imported', () => {
    expect(RoutesV1).toBeDefined();
  });
  // it('should have a method to loadRoutes', () => {
  //   expect(routes.loadRoutes).toBeDefined();
  // });
});
