import {
  STATUS_ERROR,
  STATUS_OK
} from "./healthz.const";

describe('STATUS_ERROR', () => {
  it('should be defined when imported', () => {
    // arrange
    // act
    // assert
    expect(STATUS_ERROR).toBeDefined();
  });
});

describe('STATUS_OK', () => {
  it('should be defined when imported', () => {
    // arrange
    // act
    // assert
    expect(STATUS_OK).toBeDefined();
  });
});
