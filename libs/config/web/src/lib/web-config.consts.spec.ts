import {
  WEB_ROUTES
} from './web-config.consts';

describe('WEB_ROUTES', () => {
  it('should exist when imported', () => {
    // arrange
    // act
    // assert
    expect(WEB_ROUTES).toBeDefined();
  });
});
