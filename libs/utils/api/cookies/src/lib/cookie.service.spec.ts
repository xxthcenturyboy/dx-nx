import { CookeiService } from './cookie.service';

describe('setCookies', () => {
  it('should exist as a static method when imported', () => {
    expect(CookeiService.setCookies).toBeDefined();
  });
});

describe('setCookie', () => {
  it('should exist as a static method when imported', () => {
    expect(CookeiService.setCookie).toBeDefined();
  });
});
describe('getCookie', () => {
  it('should exist as a static method when imported', () => {
    expect(CookeiService.getCookie).toBeDefined();
  });
});
describe('clearCookies', () => {
  it('should exist as a static method when imported', () => {
    expect(CookeiService.clearCookies).toBeDefined();
  });
});
describe('clearCookie', () => {
  it('should exist as a static method when imported', () => {
    expect(CookeiService.clearCookie).toBeDefined();
  });
});
