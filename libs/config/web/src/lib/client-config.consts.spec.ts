import {
  CLIENT_APP_DOMAIN,
  CLIENT_APP_URL_PORT,
  CLIENT_HTTP_PROTOCOL,
  CLIENT_APP_URL,
  FADE_TIMEOUT_DUR,
  WEB_ROUTES
} from './client-config.consts';

describe('CLIENT_APP_DOMAIN', () => {
  // arrange
  // act
  // assert
  it('should exist when imported', () => {
    expect(CLIENT_APP_DOMAIN).toBeDefined();
  });
});

describe('CLIENT_APP_URL_PORT', () => {
  // arrange
  // act
  // assert
  it('should exist when imported', () => {
    expect(CLIENT_APP_URL_PORT).toBeDefined();
  });
});

describe('CLIENT_HTTP_PROTOCOL', () => {
  // arrange
  // act
  // assert
  it('should exist when imported', () => {
    expect(CLIENT_HTTP_PROTOCOL).toBeDefined();
  });
});

describe('CLIENT_APP_URL', () => {
  // arrange
  // act
  // assert
  it('should exist when imported', () => {
    expect(CLIENT_APP_URL).toBeDefined();
  });
});

describe('FADE_TIMEOUT_DUR', () => {
  it('should exist when imported', () => {
    // arrange
    // act
    // assert
    expect(FADE_TIMEOUT_DUR).toBeDefined();
  });
});

describe('WEB_ROUTES', () => {
  it('should exist when imported', () => {
    // arrange
    // act
    // assert
    expect(WEB_ROUTES).toBeDefined();
  });
});
