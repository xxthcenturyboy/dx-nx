import {
  WEB_ROUTES
} from './client-config.consts';

describe('WEB_ROUTES', () => {
  it('should exist when imported', () => {
    // arrange
    // act
    // assert
    expect(WEB_ROUTES).toBeDefined();
  });
});
