import { dataAccessSocketIoWeb } from './data-access-socket-io-web';

describe('dataAccessSocketIoWeb', () => {
  it('should work', () => {
    expect(dataAccessSocketIoWeb()).toEqual('data-access-socket-io-web');
  });
});
